# Records

Raw personal snapshots explicitly retained by the user live here, separate from curated Personal Context pages. Each dataset uses its own directory and README describing its source, collection time, format, and verification status. Do not automatically turn every record into a profile, Goal, or person page, and never store credentials or session tokens.
